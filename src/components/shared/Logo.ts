import logo from '../../resources/drawable/logo.svg';
import styled from 'styled-components';

export const Logo = styled.div`
  background: url("${logo}") no-repeat;
  height: 40px;
  width: 40px;
`;