import styled from 'styled-components';

const Wrapper = styled.div`
  background: rgba(var(--background-400-rgb), 0.6);
  height: 100%;
  width: 100%;
`;

export function MainContent() {
    return <Wrapper>

    </Wrapper>;
}